<%- include("partials/header"); -%>

<h1>Home</h1>
<p><%= para1 %></p>

<% posts.forEach(function(post){ %>
    <h3><%= post.title %></h3>
    <i><%= post.content.substring(0, 100) + " ... " %></i><a href="/post/<%= post._id %>">Read More</a>
<% }); %>

<%- include("partials/footer"); -%>
